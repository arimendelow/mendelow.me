---
import { getCollection } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import FormattedDate from '../components/FormattedDate.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';
import { getReadingTime } from '../utils/readingTime';

const posts = (await getCollection('blog')).sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main class="pb-10">
			<section class="space-y-5 text-[1.05rem] leading-7 text-[#222]">
				<p>
					I&rsquo;m Ari Mendelow. I build software, write about product and engineering tradeoffs, and try to
					ship useful things on the internet.
				</p>
				<p>
					This site is my working notebook: notes from projects, decisions that went right or wrong, and
					small lessons worth keeping.
				</p>
				<p class="text-[0.98rem] text-[#666]">
					<a href="https://github.com/arimendelow" target="_blank" rel="noreferrer">GitHub</a>
					<span aria-hidden="true"> · </span>
					<a href="https://x.com/arimendelow" target="_blank" rel="noreferrer">Twitter/X</a>
					<span aria-hidden="true"> · </span>
					<a href="https://www.linkedin.com/in/arimendelow/" target="_blank" rel="noreferrer">LinkedIn</a>
				</p>
			</section>

			<hr class="my-10" />

			<section>
				<ul class="space-y-10">
					{
						posts.map((post) => (
							<li class="group space-y-2">
								<a
									href={`/blog/${post.id}/`}
									class="text-[1.35rem] font-semibold leading-tight text-[#111] transition-colors hover:text-[#D91C1C]"
								>
									{post.data.title}
								</a>
								<p class="text-sm text-[#666]">
									<FormattedDate date={post.data.pubDate} /> <span aria-hidden="true">&middot;</span>{' '}
									{getReadingTime(post.body)} min read
								</p>
								<p class="text-[0.98rem] text-[#666]">
									{post.data.description}
								</p>
							</li>
						))
					}
				</ul>
			</section>
			{
				posts.length === 0 && (
					<section>
						<p class="text-[#666]">
							No posts yet. The first one should land soon.
						</p>
					</section>
				)
			}
		</main>
		<Footer />
	</body>
</html>
